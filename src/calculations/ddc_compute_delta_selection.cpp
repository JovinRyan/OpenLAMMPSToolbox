#include "calculations/ddc_get_e_delta.h"

std::pair<std::vector<int>, std::vector<atom>> get_pe_del(dump_data_container in_dump, double pe_threshold)
{
  std::vector<int> pe_del_vec;
  std::vector<atom> pe_del_atoms_vec;

  return std::make_pair(pe_del_vec, pe_del_atoms_vec);
}
